function evenOrOdd(number) {
    // if dividing the number by 2 leaves no remainder
    if (number % 2 == 0) {
      // it's even
      return "Even";
    // otherwise
    } else {
      //it's odd
      return "Odd"
    }
  }
  