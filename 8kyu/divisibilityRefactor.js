function isDivisible(n, x, y) {
    // if-else loop for conditional to determine if n is divisible by x and by y
    if (n % x == 0 && n % y == 0) {
      // return true if true
      return true;
    } else {
      // return false otherwise
      return false
    }
  }
  