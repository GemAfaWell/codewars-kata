function createArray(number){
    // create a variable that is an array
    var newArray = [];
    
    //for loop to push counter to the array
    for(var counter = 1; counter <= number; counter++){
      newArray.push(counter);
    }
    
    //return the array
    return newArray;
  }
  